/** 混合侧栏展开宽度（ranknora 侧栏 260px） */
export const MIX_RAIL_WIDTH = 260

/** 混合侧栏折叠宽度（ranknora 60px） */
export const MIX_RAIL_COLLAPSED_WIDTH = 60

/** 品牌主色（ranknora --td-brand-color-4） */
export const MIX_BRAND_COLOR = '#2457a7'

/** 描边 / 分割线（ranknora --td-component-stroke） */
export const MIX_STROKE_COLOR = '#e7e7e7'

/** 选中 / 悬停背景（ranknora --td-bg-color-secondarycontainer） */
export const MIX_HIGHLIGHT_BG = '#f3f3f3'

/** 侧栏背景（ranknora --td-bg-color-sidebar） */
export const MIX_RAIL_BG = '#f9f9f9'

/** 页面内容区背景（纯白） */
export const MIX_PAGE_BG = '#ffffff'

/** 顶栏背景色（与侧栏一致，非 mix 模式顶栏用 projectSetting） */
export const MIX_HEADER_BG = MIX_RAIL_BG

/** 混合侧栏一级栏选中/悬停 */
export const MIX_RAIL_HIGHLIGHT = MIX_HIGHLIGHT_BG

/** 混合侧栏二级栏背景 */
export const MIX_SUBMENU_BG = '#ffffff'

/** 混合侧栏二级栏选中/悬停 */
export const MIX_SUBMENU_HIGHLIGHT = MIX_HIGHLIGHT_BG

/** @deprecated 使用 MIX_PAGE_BG */
export const MIX_SHELL_BG = MIX_PAGE_BG

/** 混合布局分割线 */
export const MIX_DIVIDER_COLOR = MIX_STROKE_COLOR

/** 表格表头背景 */
export const MIX_TABLE_HEADER_BG = MIX_HIGHLIGHT_BG

/** 菜单项圆角 */
export const MIX_MENU_RADIUS = 4
